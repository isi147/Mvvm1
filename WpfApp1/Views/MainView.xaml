<Window x:Class="WpfApp1.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800" WindowStartupLocation="CenterScreen">

    <Grid>
        <StackPanel>
            <TextBox x:Name="txtBox" FontSize="42" Margin="5"></TextBox>
            <Button Command="{Binding ShowCommand}" Style="{StaticResource btnStyle}">Show</Button>
            <Button Width="200" Height="70" Style="{StaticResource new}" >hello</Button>
            <Button Width="200" Height="70" Style="{StaticResource yeni}" >hello</Button>
        </StackPanel>
    </Grid>
</Window>
